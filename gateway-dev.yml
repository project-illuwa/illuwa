eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
zuul:
  routes:
    image:
      path: /api/images/**
    location:
      path: /api/locations/**
    member:
      path: /api/members/**
    notification:
      path: /api/notifications/**
    order:
      path: /api/orders/**
    payment:
      path: /api/payments/**
    review:
      path: /api/reviews/**
    store:
      path: /api/store/**